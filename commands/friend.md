---
description: Toggle robot-beep sound effects for tool events; set a per-directory voice
argument-hint: [help|on|off|status|test|voice <classic|whistle|wood|chime|list|reset>]
allowed-tools: Bash(python3:*)
---

Run this command and relay the script's output. If the output is a single line,
report it as one short line; if it is multi-line (e.g. help or a voice list),
show it verbatim in a code block. Do nothing else.

!`python3 ~/.claude/friend/friend.py $ARGUMENTS`
